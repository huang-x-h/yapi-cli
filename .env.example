# YApi CLI 环境变量配置
# 在项目根目录创建 .env 文件，填入实际值
# 加载顺序：系统环境变量 > .env 文件（高优先级覆盖低）

# YApi 服务地址（必填）
# YAPI_HOST=http://your-yapi.com

# 项目 Token（必填，从 YApi 项目设置 -> Token 管理获取）
# YAPI_PROJECT_TOKEN=xxx-your-token-xxx

# 默认创建人（可选，接口创建者的用户名）
# YAPI_USERNAME=admin
